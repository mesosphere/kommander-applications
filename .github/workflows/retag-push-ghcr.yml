name: Retag and Push Images to GHCR

on:
  workflow_dispatch:
  push:
    branches:
      - sandy/retag-valkey # branch updated for testing

jobs:
  retag-and-push:
    runs-on: 
      - self-hosted-nutanix-docker-small
    permissions:
      packages: write
      contents: read
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      # GITHUB_TOKEN cannot push to another org’s GHCR namespace. Use a PAT from a
      # user/bot with write:packages (and SSO authorized if the org uses it) on nutanix-cloud-native.
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Retag and push images
        run: |
          set -e

          IMAGES=(
            "valkey:8.1.3-debian-12-r2"
            "valkey-sentinel:8.1.3-debian-12-r2"
            "redis-exporter:1.74.0-debian-12-r3"
          )

          for IMAGE in "${IMAGES[@]}"; do
            echo "Processing $IMAGE"

            docker pull bitnamilegacy/$IMAGE
            docker tag bitnamilegacy/$IMAGE ghcr.io/mesosphere/$IMAGE
            docker push ghcr.io/mesosphere/$IMAGE
            echo "Pushed $IMAGE to ghcr.io/mesosphere"
          done
