apiVersion: v1
kind: ConfigMap
metadata:
  name: ai-navigator-rag-0.6.2-config-defaults
  namespace: ${releaseNamespace}
data:
  values.yaml: |
    openai:
      apiEndpoint: ""
    postgresql:
      priorityClassName: "dkp-critical-priority"
    auth:
      enabled: false
    image:
      tag: "0.6.2"
