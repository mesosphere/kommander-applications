apiVersion: v1
kind: ConfigMap
metadata:
  name: ai-navigator-rag-0.6.4-config-defaults
  namespace: ${releaseNamespace}
data:
  values.yaml: |
    openai:
      apiEndpoint: ""
    postgresql:
      priorityClassName: "dkp-critical-priority"
    auth:
      enabled: false
    image:
      chatbot:
        repository: "docker.io/mesosphere/ai-navigator-rag"
      tag: "0.6.4"
