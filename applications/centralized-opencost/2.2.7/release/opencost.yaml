---
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: centralized-opencost
  namespace: ${releaseNamespace}
spec:
  interval: 1m
  url: "oci://ghcr.io/opencost/charts/opencost"
  ref:
    tag: 2.2.7
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: centralized-opencost
  namespace: ${releaseNamespace}
spec:
  chartRef:
    kind: OCIRepository
    name: centralized-opencost
    namespace: ${releaseNamespace}
  interval: 15s
  install:
    crds: CreateReplace
    remediation:
      retries: 30
  upgrade:
    crds: CreateReplace
    remediation:
      retries: 30
  releaseName: centralized-opencost
  valuesFrom:
    - kind: ConfigMap
      name: centralized-opencost-2.2.7-config-defaults
  targetNamespace: ${releaseNamespace}
