---
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: external-dns
  namespace: ${releaseNamespace}
spec:
  interval: 1m
  url: "oci://ghcr.io/mesosphere/charts/external-dns"
  ref:
<<<<<<<< HEAD:applications/external-dns/9.0.0/external-dns.yaml
    tag: 9.0.0
========
    tag: 1.18.0
>>>>>>>> bf1d9fa1 (feat: update the bitnami to official dns):applications/external-dns/1.18.0/external-dns.yaml
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: external-dns
  namespace: ${releaseNamespace}
spec:
  chartRef:
    kind: OCIRepository
    name: external-dns
    namespace: ${releaseNamespace}
  interval: 15s
  install:
    crds: CreateReplace
    remediation:
      retries: 30
  upgrade:
    crds: CreateReplace
    remediation:
      retries: 30
  releaseName: external-dns
  valuesFrom:
    - kind: ConfigMap
<<<<<<<< HEAD:applications/external-dns/9.0.0/external-dns.yaml
      name: external-dns-9.0.0-config-defaults
========
      name: external-dns-1.18.0-config-defaults
>>>>>>>> bf1d9fa1 (feat: update the bitnami to official dns):applications/external-dns/1.18.0/external-dns.yaml
  targetNamespace: ${releaseNamespace}
