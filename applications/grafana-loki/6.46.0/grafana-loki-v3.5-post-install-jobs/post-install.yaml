apiVersion: batch/v1
kind: Job
metadata:
  name: grafana-loki-v3.5-post-install
  namespace: ${releaseNamespace}
spec:
  template:
    metadata:
      name: grafana-loki-v3.5-post-install
    spec:
      # serviceAccountName: grafana-loki-pre-install
      restartPolicy: OnFailure
      priorityClassName: dkp-critical-priority
      containers:
        - name: post-install
          image: "${kubetoolsImageRepository:=docker.io/mesosphere/kubectl}:${kubetoolsImageTag:=v1.34.1-alpine}"
          command:
            - sh
            - -c
            - ## TODO : read old loki data via new loki component | validation 
            - ## TODO : update the configmap of logging operator to write log data via new loki & read the new data | verification