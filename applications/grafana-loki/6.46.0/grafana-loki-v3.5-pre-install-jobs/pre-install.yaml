apiVersion: v1
kind: ServiceAccount
metadata:
  name: grafana-loki-pre-install
  namespace: ${releaseNamespace}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: grafana-loki-pre-install
  namespace: ${releaseNamespace}
rules:
  - apiGroups: ["kustomize.toolkit.fluxcd.io"]
    resources: ["kustomizations"]
    verbs: ["get", "patch"]
  - apiGroups: ["ceph.rook.io"]
    resources: ["cephclusters"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: grafana-loki-pre-install
  namespace: ${releaseNamespace}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: grafana-loki-pre-install
subjects:
  - kind: ServiceAccount
    name: grafana-loki-pre-install
    namespace: ${releaseNamespace}
---
apiVersion: batch/v1
kind: Job
metadata:
  name: grafana-loki-v3.5-pre-install
  namespace: ${releaseNamespace}
spec:
  template:
    metadata:
      name: grafana-loki-v3.5-pre-install
    spec:
      serviceAccountName: grafana-loki-pre-install
      restartPolicy: OnFailure
      priorityClassName: dkp-critical-priority
      containers:
        - name: pre-install
          image: "${kubetoolsImageRepository:=docker.io/mesosphere/kubectl}:${kubetoolsImageTag:=v1.34.1-alpine}"
          command:
            - sh
            - -c
            - ## Cephcluster wait 
            - ## TODO : add some filter to logging operator configmap
            - ## TODO : make a copy of old loki override config where depricated field will be removed and new required field will be added