---
# OCIRepository for istio-helm-base
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: ${releaseName}-base
  namespace: ${releaseNamespace}
spec:
  interval: 1m
  url: "oci://ghcr.io/mesosphere/charts/istio-helm-base"
  ref:
    tag: 1.23.6
---
# OCIRepository for istio-helm-cni
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: ${releaseName}-cni
  namespace: ${releaseNamespace}
spec:
  interval: 1m
  url: "oci://ghcr.io/mesosphere/charts/istio-helm-cni"
  ref:
    tag: 1.23.6
---
# OCIRepository for istio-helm-istiod
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: ${releaseName}-istiod
  namespace: ${releaseNamespace}
spec:
  interval: 1m
  url: "oci://ghcr.io/mesosphere/charts/istio-helm-istiod"
  ref:
    tag: 1.23.6
---
# OCIRepository for istio-helm-gateway
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: ${releaseName}-gateway
  namespace: ${releaseNamespace}
spec:
  interval: 1m
  url: "oci://ghcr.io/mesosphere/charts/istio-helm-gateway"
  ref:
    tag: 1.23.6
---
# HelmRelease for istio-helm-base (first component, no Istio dependencies)
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ${releaseName}-base
  namespace: ${releaseNamespace}
spec:
  dependsOn:
    - name: kube-prometheus-stack
      namespace: ${releaseNamespace}
  chartRef:
    kind: OCIRepository
    name: ${releaseName}-base
    namespace: ${releaseNamespace}
  interval: 15s
  install:
    crds: CreateReplace
    remediation:
      retries: 30
    createNamespace: true
  upgrade:
    crds: CreateReplace
    remediation:
      retries: 30
  releaseName: ${releaseName}-base
  targetNamespace: istio2-system
  valuesFrom:
    - kind: ConfigMap
      name: ${releaseName}-config-defaults
---
# HelmRelease for istio-helm-cni (depends on base)
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ${releaseName}-cni
  namespace: ${releaseNamespace}
spec:
  dependsOn:
    - name: ${releaseName}-base
      namespace: ${releaseNamespace}
  chartRef:
    kind: OCIRepository
    name: ${releaseName}-cni
    namespace: ${releaseNamespace}
  interval: 15s
  install:
    remediation:
      retries: 30
  upgrade:
    remediation:
      retries: 30
  releaseName: ${releaseName}-cni
  targetNamespace: kube-system
  valuesFrom:
    - kind: ConfigMap
      name: ${releaseName}-config-defaults
---
# HelmRelease for istio-helm-istiod (depends on cni)
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ${releaseName}-istiod
  namespace: ${releaseNamespace}
spec:
  dependsOn:
    - name: ${releaseName}-cni
      namespace: ${releaseNamespace}
  chartRef:
    kind: OCIRepository
    name: ${releaseName}-istiod
    namespace: ${releaseNamespace}
  interval: 15s
  install:
    remediation:
      retries: 30
  upgrade:
    remediation:
      retries: 30
  releaseName: ${releaseName}-istiod
  targetNamespace: ${releaseName}-system
  valuesFrom:
    - kind: ConfigMap
      name: ${releaseName}-config-defaults
---
# HelmRelease for istio-helm-gateway (depends on istiod)
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ${releaseName}-gateway
  namespace: ${releaseNamespace}
spec:
  dependsOn:
    - name: ${releaseName}-istiod
      namespace: ${releaseNamespace}
  chartRef:
    kind: OCIRepository
    name: ${releaseName}-gateway
    namespace: ${releaseNamespace}
  interval: 15s
  install:
    remediation:
      retries: 30
  upgrade:
    remediation:
      retries: 30
  releaseName: ${releaseName}-gateway
  targetNamespace: ${releaseName}-gateway-ns
  valuesFrom:
    - kind: ConfigMap
      name: ${releaseName}-config-defaults
