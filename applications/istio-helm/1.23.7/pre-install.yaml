apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: istio-helm-pre-install
  namespace: ${releaseNamespace}
spec:
  force: true
  prune: true
  wait: true
  interval: 10m
  retryInterval: 1m
  path: ./applications/istio-helm/1.23.7/pre-install
  sourceRef:
    kind: GitRepository
    name: management
    namespace: kommander-flux
