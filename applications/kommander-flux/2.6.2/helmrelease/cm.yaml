apiVersion: v1
kind: ConfigMap
metadata:
  name: ${releaseName}-config-defaults
  namespace: ${releaseNamespace}
data:
  values.yaml: |
    ---
    policies:
      create: true
    sourceController:
      resources:
        requests:
          cpu: 100m
          memory: 64Mi
