apiVersion: v1
kind: ConfigMap
metadata:
  name: kommander-flux-2.6.4-config-defaults
  namespace: ${releaseNamespace}
data:
  values.yaml: |
    ---
    policies:
      create: true
    sourceController:
      resources:
        requests:
          cpu: 100m
          memory: 64Mi