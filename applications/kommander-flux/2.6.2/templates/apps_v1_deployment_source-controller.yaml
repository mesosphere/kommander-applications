apiVersion: apps/v1
kind: Deployment
metadata:
  name: source-controller
  namespace: kommander-flux
  labels:
    app: source-controller
    app.kubernetes.io/instance: kommander-flux
    app.kubernetes.io/part-of: flux
  annotations:
    kustomize.toolkit.fluxcd.io/ssa: IfNotPresent
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: source-controller
      app.kubernetes.io/instance: kommander-flux
  template:
    metadata:
      labels:
        app: source-controller
        app.kubernetes.io/name: source-controller
        app.kubernetes.io/instance: kommander-flux
    spec:
      priorityClassName: "dkp-critical-priority"
      serviceAccountName: source-controller
      containers:
        - name: manager
          image: ghcr.io/fluxcd/source-controller:v1.6.1
          imagePullPolicy: IfNotPresent
          args:
            - --watch-all-namespaces=true
            - --log-level=info
            - --http-bind-address=:9090
            - --concurrent=20
          ports:
            - containerPort: 9090
              name: http-prom
          # No explicit readinessProbe to avoid flakiness during bootstrap
          resources:
            requests:
              cpu: 100m
              memory: 64Mi
