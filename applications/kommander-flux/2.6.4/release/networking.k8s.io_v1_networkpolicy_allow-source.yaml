apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-source
  namespace: ${releaseNamespace}
spec:
  ingress:
  - from:
    - namespaceSelector: {}
  podSelector:
    matchLabels:
      app.kubernetes.io/name: source-controller
  policyTypes:
  - Ingress

