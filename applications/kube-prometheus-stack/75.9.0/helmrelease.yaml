apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: kube-prometheus-stack-helmrelease
  namespace: ${releaseNamespace}
spec:
  force: false
  prune: true
  wait: true
  interval: 6h
  retryInterval: 1m
<<<<<<<< HEAD:applications/kube-prometheus-stack/71.0.1/helmrelease.yaml
  path: ./applications/kube-prometheus-stack/71.0.1/helmrelease
========
  path: ./applications/kube-prometheus-stack/75.9.0/helmrelease
>>>>>>>> a443b418 (feat: update kube-prometheus-stack to 75.9.0):applications/kube-prometheus-stack/75.9.0/helmrelease.yaml
  sourceRef:
    kind: GitRepository
    name: management
    namespace: kommander-flux
  timeout: 1m
  # passing releaseNamespace to 2nd level configuration files for ability to configure namespace correctly in attached clusters
  # Using `substituteFrom` with `substitution-vars` creates 2nd level resources in `kommander` namespace instead of workspace ns
  postBuild:
    substitute:
      releaseNamespace: ${releaseNamespace}
  healthChecks:
    - apiVersion: apps/v1
      kind: Deployment
      name: kps-kube-prometheus-stack-operator
      namespace: ${releaseNamespace}
