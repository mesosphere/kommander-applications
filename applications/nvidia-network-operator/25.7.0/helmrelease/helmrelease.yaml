apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: nvidia-network-operator
  namespace: ${releaseNamespace}
spec:
  interval: 1m
  url: "oci://ghcr.io/mesosphere/charts/network-operator"
  ref:
    tag: 25.7.0
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nvidia-network-operator
  namespace: ${releaseNamespace}
spec:
  chartRef:
    kind: OCIRepository
    name: nvidia-network-operator
    namespace: ${releaseNamespace}
  install:
    crds: CreateReplace
    createNamespace: true
    remediation:
      retries: 30
  interval: 15s
  upgrade:
    crds: CreateReplace
    remediation:
      retries: 30
  releaseName: nvidia-network-operator
  valuesFrom:
    - kind: ConfigMap
      name: nvidia-network-operator-25.7.0-config-defaults
  targetNamespace: ${releaseNamespace}
---
