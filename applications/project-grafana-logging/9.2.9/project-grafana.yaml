# Source: https://github.com/grafana/helm-charts/pkgs/container/helm-charts%2Fgrafana
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: ${releaseName}-grafana
  namespace: ${releaseNamespace}
spec:
  interval: 1m
  url: "oci://ghcr.io/grafana/helm-charts/grafana"
  ref:
    tag: 9.2.9
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: project-grafana-logging
  namespace: ${releaseNamespace}
spec:
  chartRef:
    kind: OCIRepository
    name: ${releaseName}-grafana
    namespace: ${releaseNamespace}
  interval: 15s
  install:
    crds: CreateReplace
    remediation:
      retries: 30
  upgrade:
    crds: CreateReplace
    remediation:
      retries: 30
  valuesFrom:
    - kind: ConfigMap
<<<<<<<< HEAD:applications/project-grafana-logging/8.15.2/project-grafana.yaml
      name: project-grafana-logging-8.15.2-config-defaults
========
      name: project-grafana-logging-9.2.9-config-defaults
>>>>>>>> a443b418 (feat: update kube-prometheus-stack to 75.9.0):applications/project-grafana-logging/9.2.9/project-grafana.yaml
  targetNamespace: ${releaseNamespace}
