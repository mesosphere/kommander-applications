apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: velero-pre-install
  namespace: ${releaseNamespace}
spec:
  force: true
  prune: true
  wait: true
  interval: 6h
  retryInterval: 1m
  path: ./applications/velero/11.0.0/pre-install
  sourceRef:
    kind: GitRepository
    name: management
    namespace: kommander-flux
  timeout: 1m
  postBuild:
    substitute:
      appName: ${appName}
      releaseName: ${releaseName}
      releaseNamespace: ${releaseNamespace}
      kubetoolsImageRepository: ${kubetoolsImageRepository:=bitnamilegacy/kubectl}
      kubetoolsImageTag: ${kubetoolsImageTag:=1.33.2}
