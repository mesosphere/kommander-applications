---
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: HelmRepository
metadata:
  name: ${name}
  namespace: ${namespace}
spec:
  interval: 10m
  timeout: 1m
  url: "${helmMirrorURL:=https://charts.bitnami.com/bitnami/}"
