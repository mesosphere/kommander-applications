apiVersion: v1
kind: ConfigMap
metadata:
  name: kommander-operator-values
  namespace: ${releaseNamespace}
data:
  values.yaml: |
    kommanderoperator:
      image:
        tag: "${kommanderChartVersion:=v2.18.0-vanguard.2}"
    kubetools:
      image:
        tag: "${kommanderChartVersion:=v2.18.0-vanguard.2}"
