apiVersion: v1
kind: ConfigMap
metadata:
  name: kommander-operator-values
  namespace: 
data:
  values.yaml: |
    kommanderoperator:
      image:
        tag: "${kommanderChartVersion:=v2.18.0-dev.13}"
    kubetools:
      image:
        tag: "${kommanderChartVersion:=v2.18.0-dev.13}"
