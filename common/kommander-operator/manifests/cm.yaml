apiVersion: v1
kind: ConfigMap
metadata:
  name: kommander-operator-values
  namespace: 
data:
  values.yaml: |
    kommanderoperator:
      image:
        tag: "${kommanderChartVersion:=v2.18.0-avdev.0}"
    kubetools:
      image:
        tag: "${kommanderChartVersion:=v2.18.0-avdev.0}"
