apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: kubernetes-dashboard-6.0.0-values
    namespace: ${releaseNamespace}
    files:
      - ../../services/kubernetes-dashboard/6.0.0/defaults/cm.yaml
      - default-values-kubernetes-dashboard.yaml