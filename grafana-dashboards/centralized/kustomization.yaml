apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: grafana-dashboards-centralized
    files:
      - global-overview.json
generatorOptions:
  labels:
    grafana_dashboard_kommander: "centralized"
