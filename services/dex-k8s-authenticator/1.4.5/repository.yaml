# This is the repository.yaml file for version 1.4.5 of dex-k8s-authenticator.
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: HelmRepository
metadata:
  name: dex-k8s-authenticator-staging
  namespace: kommander-flux
spec:
  interval: 10m
  timeout: 1m
  url: "${helmMirrorURL:=https://mesosphere.github.io/charts/staging}"