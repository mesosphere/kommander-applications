apiVersion: v1
kind: ConfigMap
metadata:
  name: external-dns-7.2.2-d2iq-defaults
  namespace: ${releaseNamespace}
data:
  values.yaml: |-
    priorityClassName: dkp-high-priority
    image:
      registry: ghcr.io
      repository: mesosphere/dkp-container-images/docker.io/bitnami/external-dns
      tag: 0.14.1-debian-12-r2-d2iq.0
    service:
      labels:
        servicemonitor.kommander.mesosphere.io/path: "metrics"
    metrics:
      enabled: true
