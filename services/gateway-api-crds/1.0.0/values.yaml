# Define the Gateway API CRDs to install
# crds:
#   install: true
#   resources:
#     - gatewayclasses.gateway.networking.k8s.io
#     - gateways.gateway.networking.k8s.io
#     - httproutes.gateway.networking.k8s.io
#     - tcproutes.gateway.networking.k8s.io
#     - tlsroutes.gateway.networking.k8s.io
#     - udproutes.gateway.networking.k8s.io

# # Additional settings (optional, based on your requirements)
# global:
#   namespace: "default"  # Set the namespace where CRDs will be applied. CRDs are cluster-scoped but useful for organizing workloads.

# # Resource limits for any associated jobs (if applicable)
# resources:
#   limits:
#     memory: "64Mi"
#     cpu: "100m"
#   requests:
#     memory: "32Mi"
#     cpu: "50m"

# # Labels or annotations to add to CRDs
# metadata:
#   labels:
#     app: gateway-api-crds
#   annotations:
#     managed-by: "helm"

# # Optional: Logging and debugging options (if applicable)
# logLevel: info

