---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: minio-grafana-loki-kustomization
  namespace: todo
spec:
  force: false
  interval: 6h
  path: ./minio.yaml
  - patch:
    - op: add
      path: /spec/patchesJson6902
      value: []
  postBuild:
    substitute:
      releaseNamespace: ${releaseNamespace}
    substituteFrom:
      - kind: ConfigMap
        name: minio-grafana-loki
        optional: true
