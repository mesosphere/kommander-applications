apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: kube-federation-system-namespace
  namespace: ${releaseNamespace}
spec:
  force: false
  prune: false
  wait: true
  interval: 6h
  timeout: 1m
  path: ./services/kubefed/0.9.1/kube-federation-system-namespace
  sourceRef:
    kind: GitRepository
    name: management
    namespace: kommander-flux
