apiVersion: v1
kind: ConfigMap
metadata:
  name: nkp-pulse-management-0.1.0-d2iq-defaults
  namespace: ${releaseNamespace}
data:
  values.yaml: |
    workspaceApp:
      kind: ClusterApp
      name: nkp-pulse-workspace-v0.0.1-dev.0
