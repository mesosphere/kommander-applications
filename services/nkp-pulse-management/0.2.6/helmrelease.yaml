apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: nkp-pulse-management
  namespace: ${releaseNamespace}
spec:
  chart:
    spec:
      chart: nkp-pulse-management
      sourceRef:
        kind: HelmRepository
        name: nkp-pulse-management-nkp-pulse-charts-repo
        namespace: ${releaseNamespace}
      version: v0.2.6
  interval: 15s
  releaseName: nkp-pulse-management
  targetNamespace: ${releaseNamespace}
  upgrade:
    remediation:
      strategy: uninstall
  valuesFrom:
    - kind: ConfigMap
      name: nkp-pulse-management-0.2.6-d2iq-defaults
