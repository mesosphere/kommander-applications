apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: nkp-pulse-workspace
  namespace: ${releaseNamespace}
spec:
  chart:
    spec:
      chart: nkp-pulse-workspace
      sourceRef:
        kind: HelmRepository
        name: nkp-pulse-workspace-nkp-pulse-charts-repo
        namespace: ${releaseNamespace}
      version: v0.2.6
  interval: 15s
  releaseName: nkp-pulse-workspace
  targetNamespace: ${releaseNamespace}
  upgrade:
    remediation:
      strategy: uninstall
  valuesFrom:
    - kind: ConfigMap
      name: nkp-pulse-workspace-0.2.6-d2iq-defaults
