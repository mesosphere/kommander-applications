apiVersion: v1
kind: ConfigMap
metadata:
  name: reloader-1.0.24-d2iq-defaults
  namespace: ${releaseNamespace}
data:
  values.yaml: |
    ---
    reloader:
      priorityClassName: "dkp-high-priority"
      reloadOnCreate: true
      logFormat: json
      deployment:
        resources:
          limits:
            cpu: 100m
            memory: 512Mi
          requests:
            cpu: 100m
            memory: 128Mi
