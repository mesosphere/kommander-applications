apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: reloader
  namespace: ${releaseNamespace}
spec:
  chart:
    spec:
      chart: reloader
      sourceRef:
        kind: HelmRepository
        name: stakater-github-io-stakater-charts
        namespace: kommander-flux
      version: 2.1.3
  interval: 15s
  install:
    crds: CreateReplace
    remediation:
      retries: 30
  upgrade:
    crds: CreateReplace
    remediation:
      retries: 30
  valuesFrom:
    - kind: ConfigMap
<<<<<<< HEAD
      name: reloader-2.1.2-config-defaults
=======
      name: reloader-2.1.3-d2iq-defaults
>>>>>>> 341d2a23 (chore: Update reloader 2.1.2 => 2.1.3)
    - kind: ConfigMap
      name: reloader-overrides
      optional: true
  targetNamespace: ${releaseNamespace}
